# GEMINI_API_KEY: Required for Gemini AI API calls.
# This key is used SERVER-SIDE ONLY (api/ serverless functions).
# Never expose this in frontend code or vite.config.ts define blocks.
GEMINI_API_KEY="your_gemini_api_key_here"

# APP_URL: The URL where this applet is hosted.
APP_URL="MY_APP_URL"
